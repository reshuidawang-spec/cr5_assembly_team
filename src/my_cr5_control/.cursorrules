# 机器人工程开发准则
## 1. 技术栈约束
工程背景：一名机械专业的研究生，研究方向是基于ROS2的机械臂搭载测头测针实现接触测量，现在拥有真实机械臂、测头测针，但是没有传感器，因此考虑在仿真层面先进行工作
现在的具体技术点为：基于一个箱体，手动示教或者给机械臂末端法兰三到五个点，而后通过改进RRT CONNECT算法，让机械臂末端法兰搭载测头测针通过路径规划测量这些点，将这过程中的机械臂关节姿态记录下来，工程后期考虑先进的神经网络或者算法实现优化测量，但是现在要聚焦于改进RRT CONNECT算法的改进
## 2. 技术栈约束
- 框架：ROS2 Humble
- 语言：C++ 17
- 机械臂型号：Dobot CR5
- 关键库：MoveIt2, rclcpp, tf2, Eigen

## 3. 代码风格约束
- 变量命名：使用下划线命名法（如 joint_states_sub_）
- 注释：关键算法步骤必须使用中文注释
- 错误处理：所有 ROS2 Service 调用必须包含 try-catch 或状态检查

## 4. 核心业务逻辑（不要跑偏点）
- 始终通过 `cr5_robot.hpp` 中定义的 `CR5Robot` 类来扩展功能。
- 坐标系变换：必须以 `base_link` 为基准。

